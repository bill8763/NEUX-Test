export declare class GoalTitleListItem {
    private title;
    private desc;
    constructor(title: string, desc: string);
    getTitle(): string;
    setTitle(title: string): void;
    getDesc(): string;
    setDesc(desc: string): void;
}
