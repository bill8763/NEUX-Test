import { OnInit } from '@angular/core';
import { ProfileCode, ProfileCodeService, Language } from '@allianzSND/core';
import { CustomerFilterCriteria } from '../bean/customer-filter-criteria';
import { CustomerService } from '../../service/customer-service.service';
import { Subject } from 'rxjs';
export declare class CustomerFilterComponent implements OnInit {
    private customerService;
    private profileCodeService;
    customerType: Array<ProfileCode>;
    customerDataSource: Array<ProfileCode>;
    possibility: Array<ProfileCode>;
    source: Array<ProfileCode>;
    familiarity: Array<ProfileCode>;
    recentStatus: Array<ProfileCode>;
    manpa: Array<ProfileCode>;
    gender: Array<ProfileCode>;
    marriage: Array<ProfileCode>;
    children: Array<ProfileCode>;
    income: Array<ProfileCode>;
    age: Array<ProfileCode>;
    contactFrequency: Array<ProfileCode>;
    completeness: Array<ProfileCode>;
    birthday: Array<ProfileCode>;
    isAsPresent: boolean;
    private filterColumnMap;
    clear: Subject<any>;
    save: Subject<any>;
    open: Subject<any>;
    private _criteria;
    criteria: CustomerFilterCriteria;
    private doneCriteria;
    constructor(customerService: CustomerService, profileCodeService: ProfileCodeService);
    language: Language;
    ngOnInit(): void;
    onClick(): void;
    private _resetFilter;
    private _addDefaultChecked;
    private _addCriteria;
    private _addFilterMap;
    private clearCriteria;
    private refreshFilterMap;
    private saveCriteria;
}
