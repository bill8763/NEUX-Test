import { OnInit, ElementRef, Renderer } from '@angular/core';
export declare class UiFormLayoutRowComponent implements OnInit {
    private el;
    private renderer;
    constructor(el: ElementRef, renderer: Renderer);
    ngOnInit(): void;
}
